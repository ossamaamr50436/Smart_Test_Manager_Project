# تقرير فجوة لوحة تحكم المسؤول (Admin Panel)

> نطاق التقرير: مقارنة لوحة تحكم المسؤول مقابل وثيقة المواصفات الوحيدة المعتمدة `منصة_مدير_الاختبارات_SRS_V2.md` (الإصدار 2.0).
> تمت المراجعة على: `app/(dashboard)/admin/`، `components/admin/`، `lib/actions/settings-actions.ts`، `lib/actions/admin-actions.ts`، مع ربط بـ Prisma Schema وبقية الوحدات المؤثرة.
> التاريخ: سبتمبر 2026

---

## 1. الملخص التنفيذي

حالة لوحة تحكم المسؤول الحالية: **في مرحلة البناء المبكر (Placeholder)**.

- **الوظائف الفعلية المنفذة:** شاشة واحدة فقط مكتملة — **إعدادات المنصة** (`/admin/settings`) بجانب صفحة **سجل التدقيق** (`/audit-log`).
- **الصفحة الرئيسية للمسؤول** (`/admin`) ما زالت عبارة عن قالب "قيد التطوير" ولا تعرض أي بيانات.
- **الملاحظة الأهم:** المسؤوليات الأربع المذكورة في المواصفات (البند 3.1) **غير منفذة إطلاقاً** من ناحية الواجهات: عرض بيانات المستخدمين/الجهات/الطلاب، تعديل أي بيانات، إدارة الحسابات والصلاحيات، والتقارير الشاملة.
- **فجوة بنيوية حرجة:** لا توجد في قاعدة الكود بالكامل أي عملية `create/update/delete` على جدول المستخدمين (إدارة الحسابات) — وهذا يرتبط بمواصفة إنشاء حسابات المختبرين أيضاً (البند 3.4)، فلا يقتصر الأمر على المسؤول.
- **إيجابي مُبشّر:** الطبقة الخلفية للإعدادات والتدقيق مبنية بدقة: عزل صلاحيات (ADMIN فقط)، تسجيل كل تغيير في سجل التدقيق، وتحقق من صحة الملفات (Drive). وكذلك **نظام المواسم (Seasons) موجود بالكامل في الطبقة الخلفية** (`lib/actions/season-actions.ts`) لكن بدون أي واجهة مستخدم بعد.

---

## 2. الوظائف المنفذة حالياً (Implemented)

### 2.1 إعدادات المنصة — `/admin/settings` ✅ مكتملة

| الميزة | المصدر | ملاحظات |
|--------|--------|---------|
| اسم المنصة + الشعار (رفع إلى Google Drive) | `updatePlatformSettings` | PNG/JPG/SVG فقط، حد 5MB، يُسجّل في AuditLog |
| وضع القالب الذكي للشهادات + رفع قالب PDF | `updateTemplateSettings` | PDF فقط، حد 10MB |
| الألوان الرئيسية/الثانوية | `updateAppearanceSettings` | تحقق HEX `#RRGGBB` |
| رقم واتساب الدعم الفني | `updateAppearanceSettings` | 8–15 رقم، يُنقّى من الرموز |
| الوضع المظلم | `updateAppearanceSettings` | يفعّل next-themes عبر السايدبار |
| جلب الإعدادات ديناميكياً في كل المنصة | `getPlatformSettings` (upsert ذري singleton) | مكشوف بلا مصادقة حسب التصميم (اسم/شعار عام) |

- كل هذه الدوال محمية بـ `requireRole(user, [Role.ADMIN])` وتكتب في `AppSettings` (جدول singleton).

### 2.2 سجل التدقيق — `/audit-log` ✅ مكتملة

- متاح لـ ADMIN و TEST_SPECIALIST فقط (عزل صلاحيات على مستوى الصفحة والدالة).
- تصفية حسب: نوع العملية، المستخدم، النطاق الزمني، بحث نصي في التفاصيل. +
- ترقيم صفحات، وإحصائيات عدد العمليات حسب النوع، وقائمة المستخدمين للتصفية.
- المصدر: `getAuditLogs` و `getAuditLogStats` و `getAuditLogUsers`.

### 2.3 بنى تحتية جاهزة لكن بلا واجهات (يُنصح ببناء الواجهات عليها)

| الجاهز في الخلفية | الدالة | المطلوب |
|-------------------|--------|---------|
| **إدارة مواسم الاختبارات** | `createExamSeason`, `setSeasonActive` (تفعيل موسم واحد فقط)، `getCurrentSeason`, `getExamSeasons` | واجهة إدارة المواسم (راجع الفجوة 3.4) |
| **عدادات لوحة المسؤول** | `getDashboardRedirectData` في `auth-actions.ts` (عدادات الطلاب/الجلسات حسب الدور) | شاشة تعرضها (راجع الفجوة 3.2) |

### 2.4 ما يصلح كأساس للمرحلة القادمة

- نموذج بيانات كامل في Prisma يغطي كل كيانات المواصفات: `User`, `Institution`, `Student`, `ExamSeason`, `ExamModel`, `ExamSession`, `Assessment`, `Certificate`, `Notification`, `AppSettings`, `AuditLog`.
- طبقة أمان موحدة `lib/security.ts` (`requireUser`, `requireRole`, `assertInstitutionOwnsStudent`, `assertExaminerInSession`, `assertCanAccessStudent`).
- حماية `/admin` و `/audit-log` من خلال middleware + `auth.config.ts` مع إعادة التوجيه التلقائي للدور الصحيح.

---

## 3. الفجوات مقابل المواصفات (Gaps) — مع الأولوية

مصدر الفجوات هو البند **3.1 المسؤول (Admin)**، ومرتبط بها بنود إضافية من الوثيقة.

| # | المتطلب (من الوثيقة) | الحالة | التفاصيل | الأولوية |
|---|----------------------|--------|----------|----------|
| 3.1 | **عرض جميع بيانات المستخدمين والجهات والطلاب** | ❌ غير منفذ | لا توجد صفحات قوائم/تفاصيل للمستخدمين ولا للجهات ولا للطلاب من منظور ADMIN. لا يوجد سوى التصفية الضمنية لأسماء المستخدمين داخل سجل التدقيق | **حرجة (P1)** |
| 3.1 | **تعديل أي بيانات في النظام بدون قيود** | ❌ غير منفذ | الوحيد القابل للتعديل هو `AppSettings`. لا يوجد تعديل للطلاب، الجهات، المستخدمين، النماذج، أو الحالات | **حرجة (P1)** |
| 3.1 | **إنشاء/حذف الحسابات وإدارة الصلاحيات** | ❌ **غير منفذ إطلاقاً** | لا توجد أي `prisma.user.create/update/delete` في كامل كود الإنتاج. هذا يحجب أيضاً قدرة الأخصائي على إنشاء حسابات المختبرين في المواصفة (البند 3.4) | **حرجة (P1)** |
| 3.1 | **عرض التقارير الشاملة عن حالة المنصة** | ❌ غير منفذ | لا توجد شاشة تقارير/لوحة إحصائية. الجاهز الوحيد: `getAuditLogStats` وعدادات `getDashboardRedirectData` | **عالية (P2)** |
| 3.4 / §4.1 | **إضافة النماذج الاختبارية يدوياً (100 نموذج)** | ⚠️ جزئي/مخفي | الإنشاء يتم **تلقائياً فقط** عند اختيار المختبر رقماً غير موجود في `assessment-actions.ts` (سطر 89) — لا توجد واجهة إدارة نماذج للأخصائي/المسؤول | **عالية (P2)** |
| §6 / §7.2 | **مواسم الاختبارات (مثل: 1447هـ - الفصل الأول)** | ⚠️ خلفية فقط | الدوال جاهزة (`season-actions.ts`) لكن **لا توجد أي واجهة** تستدعيها (لا `.tsx` يشير إليها) | **عالية (P2)** |
| §4.2 | **معايير التقييم قابلة للتعديل (خطأ/شك/تجويد)** | ❌ غير منفذ | الخصومات ثابتة في الكود (-1، -0.5، -0.25) ولا يمكن للأخصائي/المسؤول تعديلها | **متوسطة (P3)** |
| §5 | **حساب أقدمية المعلمين تلقائياً حسب تاريخ الميلاد** | ⚠️ غير مؤكد واجهياً | الحقول موجودة في المخطط (`birthDate`, `sessionsAsTeacher1/2`) لكن لا توجد شاشة إنشاء لجان تحدد المعلم الأكبر/الأصغر واجهياً | **متوسطة (P3)** |

### أولويات مقترحة للتطبيق

- **P1 (حرجة — تمنع إطلاق الموسم القادم):** إدارة المستخدمين والصلاحيات، لوحة المسؤول الرئيسية، عرض/تعديل بيانات الجهات والطلاب.
- **P2 (عالية — اكتمال العملية التشغيلية):** واجهة المواسم، واجهة النماذج الـ 100، التقارير الشاملة.
- **P3 (متوسطة — تحسين الجودة):** تعديل معايير التقييم، واجهة لجان صريحة حسب الأقدمية.

---

## 4. التوصيات (على مراحل)

### المرحلة 1: الأساس (يُنهي الفجوات الحرجة P1)

1. **لوحة المسؤول الرئيسية** (`/admin`): كروت إحصائية (عدد المستخدمين/الجهات/الطلاب/الجلسات/الشهادات حسب الحالة) — تُبنى على `getDashboardRedirectData` وتضاف دوال جرّد للـ ADMIN.
2. **إدارة المستخدمين والحسابات** (`/admin/users`): إنشاء/تعديل/حذف مستخدم، اختيار الدور، وربط الجهة لمستخدمي INSTITUTION — مع تسجيل AuditLog لكل عملية واستخدام `lib/security.ts`.
3. **الجهات** (`/admin/institutions`): قائمة/تفاصيل/تعديل بيانات الجهات مع عدد طلابها ومستخدميها.
4. **الطلاب** (`/admin/students`): قائمة قابلة للبحث/الفلترة حسب الجهة والحالة والفرع، مع تفاصيل وعرض/تعديل البيانات.

### المرحلة 2: العمليات (تغلق P2)

5. **إدارة المواسم** (`/admin/seasons`): إنشاء موسم ببداية/نهاية، وتفعيل موسم واحد نشط — الاستفادة المباشرة من `season-actions.ts` الجاهزة وبناء نموذج (Form) فقط.
6. **التقارير الشاملة** (`/admin/reports`): حالة كل مرحلة من مراحل سير العمل الثمانية في الوثيقة (ترشيحات → لجان → جلسات → اعتماد → شهادات)، مع تصدير أساسي، ودمج إحصائيات سجل التدقيق.
7. **إدارة النماذج** (`/admin/models`): إنشاء النماذج (رقم 1–100 + السور/الآيات) يدوياً للأخصائي/المسؤول بدلاً من الإنشاء الضمني — المخطط يمنع تكرار النموذج للموسم/الجهة بالفعل (`@@unique`).

### المرحلة 3: التحسين (P3 + استكمال)

8. **معايير التقييم القابلة للتعديل**: تحويل الخصومات الثابتة إلى إعدادات قابلة للتحديث (توسعة `AppSettings` أو جدول منفصل).
9. **واجهة لجان صريحة حسب الأقدمية**: استيفاء تاريخ الميلاد وحساب المعلم الأكبر/الأصغر تلقائياً (يتماشى مع البند 5 في الوثيقة).
10. **فحص إضافي لكل أدوار الوثيقة** وربطها بلوحة المسؤول (إضافة روابط مخصصة حسب الدور في السايدبار).

### ملاحظات تقنية عامة (توصية، خارج نطاق لوحة المسؤول مباشرة)

- النماذج تُخزن كـ JSON في Neon (`detailsJSON`) بينما تنص الوثيقة (البند 7.1) على تخزين ملفات النماذج (JSON) في Google Drive — توثيق للأمر لتقرير لاحق.
- المزامنة الحية (WebSocket < 500ms) في البند 9.1 غير منفذة — خارج نطاق لوحة المسؤول لكنها ستؤثر على المعلمين.
- متطلبات الأمان الأساسية المطبقة بشكل جيد فعلاً: bcrypt، JWT، عزل صلاحيات على مستوى Server Action، سجل تدقيق لكل تغيير، ومصادقة CSP في الإنتاج.

---

## 5. خلاصة

| البند | النتيجة |
|-------|---------|
| الشاشات المكتملة في لوحة المسؤول | 1 (الإعدادات) + سجل التدقيق |
| الفجوات الحرجة (P1) | 3 (إدارة الحسابات، عرض/تعديل البيانات، لوحة رئيسية وتقارير) |
| الوحدات الخلفية الجاهزة للربط المباشر | إعدادات المنصة، سجل التدقيق، المواسم، العدادت |
| التوصية العامة | البدء بالمرحلة 1 بناءً على `lib/security.ts` ونموذج Prisma الكامل، مع تسجيل كل عملية في AuditLog كالمعتاد الحالي |

**الخلاصة:** البنية التحتية للقواعد والأمان ممتازة، لكن واجهة المسؤول نفسها لا تغطي سوى أقل من 10% من المسؤوليات الأربع في المواصفة، وأهم أولوية فنية هي بناء شاشة إدارة المستخدمين والصلاحيات لأنها ترتبط بالكامل ببقية أدوار الحوكمة (المختبرين، رئيس الشؤون، مصدر الشهادات).