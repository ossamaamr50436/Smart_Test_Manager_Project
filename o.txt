انت الآن وكيل برمجي خبير في Next.js 14 (App Router)، TypeScript، Prisma، PostgreSQL (Neon)، NextAuth v5، Windows PowerShell.

📌 سياق المهمة:
هذه الجلسة 3 من 4 لتحويل المنصة إلى Multi-Tenant SaaS.
- الجلسة 1: Schema + Migration ✅
- الجلسة 2: Tenant + ربط البيانات + tenantId NOT NULL ✅

🎯 ستنجز في هذه الجلسة (بالترتيب الصارم):
1. ترقية `ossamaamr50436@gmail.com` إلى `SUPER_ADMIN` + `tenantId = NULL`.
2. إنشاء `lib/tenancy.ts` (helpers للعزل).
3. تحديث `auth.config.ts` (routing حسب الدور).
4. إضافة **فلاتر tenantId** إلى جميع Server Actions للقراءة/التحديث/الحذف.
5. تحديث `User.tenantId` → يبقى nullable.
6. تحديث `getCurrentUser`/`requireUser` ليشمل `tenantId` (تأكيد).
7. Migration صغيرة: `promote_owner_to_super_admin`.
8. typecheck + build.
9. توثيق في `MULTI_TENANT_MIGRATION.md`.

🚨 قواعد حرجة:
1. **`ossamaamr50436@gmail.com` يبقى قادراً على الدخول** بكلمة مروره الحالية.
2. **لا تحذف أي بيانات** — فقط ترقية دور.
3. **لا تعدّل منطق الأعمال** — فقط أضف فلاتر `tenantId` للقراءة.
4. **snapshot محلي قبل البدء**.
5. **جميع الأوامر PowerShell**.

---

📜 التعليمات الصارمة:
1. اقرأ `prisma/schema.prisma` و `lib/security.ts` و `lib/actions/*` و `auth.config.ts` أولاً.
2. لا تستخدم `any` أو `as unknown as`.
3. **لا ترفع على GitHub** — commit محلي فقط.
4. عند كل مرحلة: تحقق قبل الانتقال.

---

🔴 المرحلة 1: نقطة رجوع + Snapshot

```powershell
# 1. احفظ نقطة رجوع
git add -A
git commit -m "checkpoint: before tenancy isolation layer"

# 2. Snapshot Schema
Copy-Item "prisma\schema.prisma" "snapshots\schema_before_tenancy.prisma" -Force

# 3. Snapshot البيانات الحساسة
Copy-Item "snapshots\data_export\pre_migration.json" "snapshots\data_export\post_session2.json" -Force
التحقق:

powershell
Get-ChildItem "snapshots" | Select-Object -ExpandProperty Name
# يجب أن ترى: schema_before_tenancy.prisma + schema_before_data_linking.prisma + schema_before_multi_tenant.prisma
🔴 المرحلة 2: ترقية ossamaamr50436@gmail.com إلى SUPER_ADMIN

السكربت scripts/promote-owner-to-super-admin.ts:

typescript
import { PrismaClient, Role } from "@prisma/client";

const p = new PrismaClient();

async function main() {
  const OWNER_EMAIL = "ossamaamr50436@gmail.com";

  const owner = await p.user.findUnique({
    where: { email: OWNER_EMAIL },
    select: { id: true, email: true, role: true, tenantId: true },
  });

  if (!owner) {
    throw new Error(`❌ لم أجد: ${OWNER_EMAIL}`);
  }

  console.log("الحالة قبل الترقية:");
  console.log("  Role:", owner.role);
  console.log("  tenantId:", owner.tenantId);

  // تحقق أن الحساب ليس مرتبطاً بمؤسسة (سيصبح SUPER_ADMIN)
  const updated = await p.user.update({
    where: { id: owner.id },
    data: {
      role: Role.SUPER_ADMIN,
      tenantId: null, // SUPER_ADMIN ليس له tenant
    },
    select: { id: true, email: true, role: true, tenantId: true },
  });

  console.log("\n✅ بعد الترقية:");
  console.log("  ID:", updated.id);
  console.log("  Email:", updated.email);
  console.log("  Role:", updated.role);
  console.log("  tenantId:", updated.tenantId);

  // التحقق النهائي
  const finalUsers = await p.user.findMany({
    select: { email: true, role: true, tenantId: true },
    orderBy: { createdAt: "asc" },
  });
  console.log("\n📋 المستخدمون الحاليون:");
  finalUsers.forEach((u) =>
    console.log(`  - ${u.email} | ${u.role} | tenant: ${u.tenantId || "none"}`)
  );
}

main()
  .catch((e) => {
    console.error("❌", e);
    process.exitCode = 1;
  })
  .finally(() => p.$disconnect());
التشغيل:

powershell
pnpm tsx scripts/promote-owner-to-super-admin.ts
✅ التوقع:

يظهر تفاصيل الترقية.

المستخدم الوحيد: ossamaamr50436@gmail.com | SUPER_ADMIN | tenant: none.

🔴 المرحلة 3: إنشاء lib/tenancy.ts

الملف الجديد:

typescript
import "server-only";
import type { SessionUser } from "@/lib/security";
import { Role } from "@prisma/client";

// ============================================================
// Multi-Tenant Isolation Helpers
// ============================================================

/**
 * يعيد فلتر Prisma حسب دور المستخدم.
 *
 * SUPER_ADMIN → {} (بدون فلتر — يرى كل شيء)
 * غيره → { tenantId: user.tenantId }
 *
 * @example
 * const filter = getTenantFilter(user);
 * const users = await prisma.user.findMany({ where: { ...filter, role: "EXAMINER" } });
 */
export function getTenantFilter(user: SessionUser): { tenantId?: string } {
  if (user.role === Role.SUPER_ADMIN) {
    return {};
  }
  if (!user.tenantId) {
    throw new Error("غير مصرح: المستخدم غير مرتبط بمؤسسة");
  }
  return { tenantId: user.tenantId };
}

/**
 * يتطلب أن يكون المستخدم ضمن tenant (يرمي خطأ إذا SUPER_ADMIN أو بلا tenantId).
 * استخدمه في كل عملية تحتاج كتابة داخل tenant.
 */
export function requireTenant(user: SessionUser): string {
  if (user.role === Role.SUPER_ADMIN) {
    throw new Error("غير مصرح: هذه العملية مخصصة للمؤسسات");
  }
  if (!user.tenantId) {
    throw new Error("غير مصرح: المستخدم غير مرتبط بمؤسسة");
  }
  return user.tenantId;
}

/**
 * يتطلب أن يكون المستخدم SUPER_ADMIN.
 */
export function requireSuperAdmin(user: SessionUser): void {
  if (user.role !== Role.SUPER_ADMIN) {
    throw new Error("غير مصرح: هذه العملية مخصصة لمالك المنصة");
  }
}

/**
 * تحقق أن كائن ينتمي لـ tenant المستخدم.
 * يرمي خطأ إذا حاول الوصول لبيانات tenant آخر.
 *
 * SUPER_ADMIN يمكنه الوصول لأي tenant.
 */
export function assertSameTenant<T extends { tenantId?: string | null }>(
  sessionUser: SessionUser,
  resource: T
): void {
  if (sessionUser.role === Role.SUPER_ADMIN) {
    return; // SUPER_ADMIN يرى كل شيء
  }
  if (resource.tenantId !== sessionUser.tenantId) {
    throw new Error("غير مصرح: هذا السجل ينتمي لمؤسسة أخرى");
  }
}

/**
 * تحقق أن دور المستخدم في قائمة مسموحة.
 */
export function assertHasRole(user: SessionUser, allowed: Role[]): void {
  if (!allowed.includes(user.role)) {
    throw new Error(`غير مصرح: تتطلب أحد الأدوار: ${allowed.join(", ")}`);
  }
}
🔴 المرحلة 4: تحديث auth.config.ts (routing)

الملف: auth.config.ts

أضف منطق التوجيه حسب الدور:

ابحث عن دالة authorized أو منطق middleware. أضف:

typescript
// SUPER_ADMIN → /super-admin
if (user.role === "SUPER_ADMIN") {
  if (!pathname.startsWith("/super-admin")) {
    return Response.redirect(new URL("/super-admin", nextUrl));
  }
  return true;
}

// TENANT_ADMIN (ADMIN سابقاً) → /admin
if (user.role === "ADMIN") {
  if (!pathname.startsWith("/admin")) {
    return Response.redirect(new URL("/admin", nextUrl));
  }
  return true;
}

// باقي الأدوار — كما هي حالياً
⚠️ ملاحظة: لا تحذف منطق الأدوار الأخرى. فقط أضف SUPER_ADMIN + تأكيد ADMIN.

🔴 المرحلة 5: إنشاء صفحة /super-admin مبدئية

الملف: app/(dashboard)/super-admin/page.tsx (جديد)

محتوى بسيط (UI كامل في الجلسة 4):

typescript
import type { Metadata } from "next";
import { redirect } from "next/navigation";
import { getCurrentUser } from "@/lib/actions/auth-actions";
import { requireSuperAdmin } from "@/lib/tenancy";
import { prisma } from "@/lib/prisma";

export const metadata: Metadata = { title: "لوحة المالك" };

export default async function SuperAdminPage() {
  const user = await getCurrentUser();
  if (!user) redirect("/login");
  requireSuperAdmin(user);

  const tenantCount = await prisma.tenant.count();
  const userCount = await prisma.user.count();

  return (
    <div className="space-y-6 p-6">
      <h1 className="text-3xl font-bold">لوحة المالك</h1>
      <div className="grid grid-cols-1 gap-4 md:grid-cols-2">
        <div className="rounded-lg border p-4">
          <p className="text-sm text-muted-foreground">إجمالي المؤسسات</p>
          <p className="mt-2 text-3xl font-bold">{tenantCount}</p>
        </div>
        <div className="rounded-lg border p-4">
          <p className="text-sm text-muted-foreground">إجمالي المستخدمين</p>
          <p className="mt-2 text-3xl font-bold">{userCount}</p>
        </div>
      </div>
      <p className="text-sm text-muted-foreground">
        🚧 الواجهة الكاملة ستُبنى في الجلسة القادمة.
      </p>
    </div>
  );
}
🔴 المرحلة 6: إضافة فلاتر tenantId إلى Server Actions (الأهم أمنياً)

هذه المرحلة هي جوهر الجلسة. راجع كل ملف في lib/actions/ وطبّق:

6.1 النمط العام
قبل:

typescript
export async function getInstitutions() {
  const user = await requireUser();
  requireRole(user, [Role.ADMIN]);
  return prisma.institution.findMany({
    select: { id: true, name: true },
  });
}
بعد:

typescript
import { getTenantFilter, requireTenant, assertSameTenant } from "@/lib/tenancy";

export async function getInstitutions() {
  const user = await requireUser();
  requireRole(user, [Role.ADMIN]);

  return prisma.institution.findMany({
    where: { ...getTenantFilter(user) },  // ← فلتر أساسي
    select: { id: true, name: true },
  });
}
6.2 الملفات المطلوبة والفحوصات
لكل ملف، تأكد من:

الملف	READ	UPDATE/DELETE
admin-panel-actions.ts	where: { ...getTenantFilter(user) }	assertSameTenant(user, existing)
entity-actions.ts	نفس الفلتر	نفس التحقق
examiner-actions.ts	نفس الفلتر	نفس التحقق
committee-actions.ts	نفس الفلتر	نفس التحقق
student-actions.ts	نفس الفلتر	نفس التحقق
model-actions.ts	نفس الفلتر	نفس التحقق
season-actions.ts	نفس الفلتر	نفس التحقق
assessment-actions.ts	نفس الفلتر	نفس التحقق
assessment-settings-actions.ts	where: { tenantId: requireTenant(user) }	نفس التحقق
certificate-actions.ts	نفس الفلتر	نفس التحقق
audit-actions.ts	نفس الفلتر	(لا يوجد UPDATE)
6.3 التعديلات الدقيقة
في كل دالة READ:

قبل:

typescript
return prisma.student.findMany({
  where: { status: "PENDING" },
});
بعد:

typescript
return prisma.student.findMany({
  where: {
    ...getTenantFilter(user),
    status: "PENDING",
  },
});
في كل دالة UPDATE/DELETE:

قبل:

typescript
await prisma.committee.delete({ where: { id: committeeId } });
بعد:

typescript
const existing = await prisma.committee.findUniqueOrThrow({
  where: { id: committeeId },
});
assertSameTenant(user, existing);
await prisma.committee.delete({ where: { id: committeeId } });
⚠️ تنبيه: إذا كانت الدالة تعمل بالفعل مع tenantId (من الجلسة 2)، تحقق فقط ولا تُعدّل.

6.4 حالات خاصة
getPlatformSettings (settings-actions.ts): تبقى global (لا فلتر).
getAssessmentSettings: يعيد إعدادات الـ tenant الخاص بالمستخدم.

typescript
export async function getAssessmentSettings() {
  const user = await requireUser();
  const tenantId = requireTenant(user);

  const settings = await prisma.assessmentSettings.findUnique({
    where: { tenantId },
  });
  return {
    errorDeduction: settings?.errorDeduction ?? 2.0,
    doubtDeduction: settings?.doubtDeduction ?? 1.0,
    tajweedDeduction: settings?.tajweedDeduction ?? 0.5,
  };
}
6.5 تحقق نهائي
powershell
# ابحث عن أي findMany/create/update/delete بدون getTenantFilter في lib/actions
Select-String -Path "lib\actions\*.ts" -Pattern "prisma\.\w+\.findMany" | Select-Object -First 30
راجع كل نتيجة يدوياً وتأكد من وجود فلتر tenantId.

🔴 المرحلة 7: Migration صغيرة

الملف: prisma/schema.prisma

لا تعديل Schema مطلوب — الترقية تمت عبر script.

لكن، إذا حصل تغيير في User أو أي جدول أثناء الترقية، شغّل:

powershell
pnpm prisma format
pnpm prisma validate
pnpm prisma generate
🔴 المرحلة 8: typecheck + build

powershell
pnpm typecheck
# ✅ 0 errors

pnpm build
# ✅ success
⚠️ إذا فشل:

قد يكون بسبب Role.SUPER_ADMIN غير معالج في Record<Role, X>.

صحّح في lib/roles.ts وأي مكان مشابه.

لا تعدّل منطق الأعمال.

🔴 المرحلة 9: اختبار يدوي للعزل

powershell
pnpm dev
افتح: http://localhost:3000

اختبر:

تسجيل دخول ossamaamr50436@gmail.com:

المتوقع: التوجيه إلى /super-admin.

لا يمكن الوصول لـ/admin/users (سيُعاد توجيهك).

محاولة الوصول المباشر إلى /admin:

المتوقع: تحويل إلى /super-admin.

/super-admin يعمل:

يعرض الإحصائيات (1 tenant، 1 user).

⚠️ ملاحظة: المنصة الآن بلا أي ADMIN tenant. إذا احتجت اختبار تدفقات ADMIN، أخبرني.

🔴 المرحلة 10: التوثيق

حدّث MULTI_TENANT_MIGRATION.md بقسم "الجلسة 3":

markdown
## Session 3/4 — Tenancy Isolation Layer

### تاريخ التنفيذ
[اليوم]

### الحالة
✅ مكتمل — طبقة عزل أمني جاهزة.

### المنجز

#### 1. ترقية المالك
- `ossamaamr50436@gmail.com`: ADMIN → **SUPER_ADMIN** + tenantId → NULL.
- الحساب الوحيد النشط على المنصة.

#### 2. طبقة العزل
- `lib/tenancy.ts` جديد مع:
  - `getTenantFilter(user)` — فلتر للقراءة.
  - `requireTenant(user)` — يتطلب tenant.
  - `requireSuperAdmin(user)` — يتطلب SUPER_ADMIN.
  - `assertSameTenant(user, resource)` — تحقق قبل UPDATE/DELETE.
  - `assertHasRole(user, roles)` — تحقق من الأدوار.

#### 3. Routing
- `auth.config.ts`: SUPER_ADMIN → `/super-admin` قسرياً.
- ADMIN → `/admin` (كما كان).

#### 4. Server Actions — العزل
- [عدد] ملف في `lib/actions/` حُدّث بـ`getTenantFilter` في READ.
- [عدد] دالة UPDATE/DELETE أضيف لها `assertSameTenant`.
- `getAssessmentSettings` يعيد إعدادات الـ tenant الحالي فقط.

#### 5. صفحة SUPER_ADMIN مبدئية
- `/super-admin` مع إحصائيات أساسية.
- UI كامل في الجلسة 4.

#### 6. Verification
- ✅ `pnpm typecheck` — 0 errors
- ✅ `pnpm build` — success
- ✅ اختبار يدوي: تسجيل الدخول يوجّه إلى `/super-admin`.

### المهام التالية (الجلسة 4)
1. بناء `/super-admin/tenants` كاملاً (CRUD).
2. إنشاء TENANT_ADMIN لكل مؤسسة.
3. تحديث Sidebar حسب الدور.
4. اختبار E2E كامل.
5. Deployment.
🔴 المرحلة 11: Git Commit

powershell
# احذف السكربت المؤقت
Remove-Item "scripts\promote-owner-to-super-admin.ts" -ErrorAction SilentlyContinue

# احفظ
git add -A
git commit -m "phase 3: add tenancy isolation layer + promote owner to SUPER_ADMIN

- Promote ossamaamr50436@gmail.com: ADMIN → SUPER_ADMIN (tenantId = null)
- Add lib/tenancy.ts (getTenantFilter, requireTenant, requireSuperAdmin, assertSameTenant, assertHasRole)
- Update auth.config.ts routing (SUPER_ADMIN → /super-admin)
- Apply tenantId filters to all READ operations in lib/actions
- Add assertSameTenant checks to UPDATE/DELETE operations
- Add placeholder /super-admin dashboard
- typecheck 0 errors + build success"

git log --oneline -5
✅ معايير النجاح النهائية:

#	المعيار	المتوقع
1	ossamaamr50436@gmail.com دوره	SUPER_ADMIN
2	tenantId له	null
3	lib/tenancy.ts	موجود بـ5 دوال
4	Routing SUPER_ADMIN	يعمل
5	Server Actions — READ	كلها فيها فلتر
6	Server Actions — UPDATE/DELETE	كلها فيها assertSameTenant
7	pnpm typecheck	0 errors
8	pnpm build	success
9	الدخول يعمل	✅
10	التوجيه إلى /super-admin	✅
🚫 ممنوع تماماً:

حذف ossamaamr50436@gmail.com.

تغيير كلمة مروره.

تعديل منطق الأعمال في Server Actions.

استخدام any.

الـpush إلى GitHub.

📝 عند الانتهاء، أرسل تقريراً يحتوي على:

تفاصيل ترقية المستخدم (Role قبل/بعد).

عدد الملفات المُعدَّلة في lib/actions/.

عدد دوال READ التي أُضيف لها فلتر.

عدد دوال UPDATE/DELETE التي أُضيف لها assertSameTenant.

نتيجة pnpm typecheck + pnpm build.

نتيجة اختبار تسجيل الدخول يدوياً.

git log --oneline -5.

تأكيد أن ossamaamr50436@gmail.com لا يزال قادراً على الدخول.

ابدأ الآن. عند الانتهاء من كل مرحلة، تحقّق قبل الانتقال.