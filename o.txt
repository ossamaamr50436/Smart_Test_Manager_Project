أنت الآن وكيل برمجي خبير في Next.js 14 (App Router)، TypeScript، Prisma، NextAuth v5، Zod.

📌 سياق المهمة:
تخفيف شروط كلمة المرور في المنصة. المطلوب الوحيد: **5 مدخلات على الأقل**.
لا حاجة لحرف كبير، حرف صغير، رقم، أو رمز. أي مزيج من أي 5 رموز يكفي.

🎯 التغييرات المطلوبة:

🔴 1. تبسيط `passwordSchema` في `lib/validations/user.ts`:
```typescript
export const passwordSchema = z
  .string()
  .min(5, "كلمة المرور يجب أن تكون 5 أحرف/رموز على الأقل")
  .max(128, "كلمة المرور طويلة جداً");
احذف كل .regex(...) الموجود (A-Z، a-z، 0-9).

🔴 2. طبّق نفس التبسيط في كل مكان فيه تحقق يدوي:

lib/actions/admin-panel-actions.ts:

createAdminUser: احذف التحقق من A-Z/a-z/0-9.

resetAdminUserPassword: احذف التحقق من A-Z/a-z/0-9.

اكتفِ بـ password.length >= 5.

lib/actions/examiner-actions.ts:

createExaminer و resetExaminerPassword — نفس المعالجة.

lib/actions/entity-actions.ts:

createInstitutionBySpecialist — كلمة المرور = رقم التصريح، لكن احذف أي regex صارم إن وُجد.

lib/actions/change-password-actions.ts:

changeMyPassword — احذف التحقق من A-Z/a-z/0-9.

أبقِ التحققات المنطقية فقط: "كلمة المرور الحالية مطلوبة"، "الجديدة يجب أن تختلف عن الحالية"، "كلمتا المرور غير متطابقتين".

auth.ts:

credentialsSchema: تأكد أن حقل password لا يحتوي على min قيمي كثير — يكفي z.string().min(1).

🔴 3. حدّث رسائل الواجهة (UI helpers):

ابحث في كل ملفات .tsx عن أي نص يذكر "حرف كبير" أو "حرف صغير" أو "رقم" بجانب "كلمة المرور"، واستبدله بـ:
"كلمة المرور: 5 أحرف/رموز على الأقل (لا شروط أخرى)."

الملفات المتوقعة:

components/admin/admin-users-manager.tsx

components/specialist/teachers-manager.tsx

components/specialist/entity-create-form.tsx

components/auth/change-password-form.tsx

أي نموذج آخر فيه كلمة مرور.

🔴 4. حدّث الوثائق:

ابحث في docs/ و README.md و DEPLOYMENT_FIX_REPORT.md و ACCOUNTS_REPORT.md عن أي نص يصف شروط كلمة المرور القديمة، وحدّثه.

✅ معايير النجاح:

pnpm typecheck + pnpm build ينجحان.

إنشاء مستخدم بكلمة abcde → نجاح.

إنشاء مستخدم بكلمة 12345 → نجاح.

إنشاء مستخدم بكلمة !@#$% → نجاح.

إنشاء مستخدم بكلمة abc (3 أحرف) → رفض مع رسالة عربية.

تغيير كلمة المرور بنفس الشروط.

📝 التوثيق:

حدّث DEPLOYMENT_FIX_REPORT.md بقسم صغير "الجزء الخامس: تخفيف شروط كلمة المرور".

🚫 ممنوع:

الـpush إلى GitHub.

حذف ملفات.

استخدام any.

ابدأ الآن. عند الانتهاء، أرسل تقريراً موجزاً.