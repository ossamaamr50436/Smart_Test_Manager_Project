Phase B مكتملة بنجاح — شكراً على إصلاح أخطاء analyze-performance.ts.

ابدأ Phase C (الأمان الثلاثي) + Phase D (التوثيق) — مع قرار معماري إلزامي:

## 🎯 قرار معماري إلزامي قبل البدء

بالنسبة لـRLS (Layer 2):
- أنشئ migration enable_row_level_security بصيغة --create-only.
- ✅ نفّذ: كتابة الـmigration + RLS policies في الملف.
- ❌ لا تنفّذ: prisma migrate deploy لهذا الـmigration.
- السبب: RLS مع Neon Pooler يحتاج withTenantContext في كل Server Action
  — وهذا عمل ضخم يؤجل للإصدار التالي. الحالي: جاهز للتطبيق لاحقاً.

## 📋 Layer 1: Authentication Hardening

1. auth.config.ts:
   - session: maxAge 8h, updateAge 1h.
   - Cookies: __Secure-next-auth.session-token + httpOnly + sameSite lax + secure.
2. auth.ts: Account Lockout
   - 5 محاولات فاشلة → قفل 15 دقيقة.
   - التحقق قبل مقارنة كلمة المرور.
   - سجّل في AuditLog كـFAILED_LOGIN.
3. lib/rate-limit.ts: checkMultiLevelRateLimit(user, ip, action)
   - IP: 100 req / window
   - User: 50 req / window
   - Tenant: 500 req / window

## 📋 Layer 2: RLS (بدون تطبيق)

1. أضف enum AuditAction القيم الجديدة:
   - SUSPICIOUS_ACCESS, RATE_LIMIT_HIT, CROSS_TENANT_ATTEMPT, FAILED_LOGIN
   - Migration add_security_audit_actions (طبّقها فعلاً — enum فقط).
2. أنشئ migration enable_row_level_security بصيغة --create-only:
   - ENABLE ROW LEVEL SECURITY على 11 جدول.
   - CREATE POLICY tenant_isolation_<table> لكل جدول.
   - ⚠️ لا تطبّقها بـmigrate deploy.
3. أنشئ lib/tenancy-db.ts مع withTenantContext(user, fn) — utility جاهز
   لكن غير مستخدم بعد.

## 📋 Layer 3: Intrusion Detection + Alerting

1. lib/security-alerts.ts: raiseSecurityAlert(payload)
   - AuditLog (tenantId: null).
   - Pusher channel: private-super-admin-alerts.
   - Notifications لكل SUPER_ADMIN.
   - فشل التنبيه لا يُفشل الطلب (try/catch).
2. حقن التنبيهات في:
   - lib/tenancy.ts → assertSameTenant → CROSS_TENANT_ATTEMPT.
   - lib/rate-limit.ts → RATE_LIMIT_HIT.
   - auth.ts → FAILED_LOGIN.
3. app/api/pusher/auth/route.ts:
   - private-super-admin-alerts حصري لـSUPER_ADMIN.
4. تأكد من عمل صفحة /super-admin/alerts.

## 📋 Hardening إضافي

1. Security Headers في next.config.mjs (8 headers):
   - X-Frame-Options, X-Content-Type-Options, Referrer-Policy, HSTS,
     Permissions-Policy, X-DNS-Prefetch-Control, X-Download-Options,
     X-Permitted-Cross-Domain-Policies.
2. pnpm audit --audit-level=moderate
   - Critical/High مباشر: حدّث.
   - Critical/High transitive: وثّق.
   - Moderate: وثّق فقط.

## 📋 Phase D: التوثيق (4 ملفات)

1. MULTI_TENANT_MIGRATION.md — قسم Session 4/4.
2. SECURITY_ARCHITECTURE.md (جديد):
   - الطبقات الثلاث.
   - جدول التهديدات (12+).
   - قرار RLS + السبب.
   - قسم "Limitations الصريحة" (لا يمكن ضمان اختراق صفري 100%).
3. PERFORMANCE_BENCHMARKS.md (محدَّث).
4. PENETRATION_TEST_CHECKLIST.md (جديد): 8 أقسام.

## ⚠️ حرج

- لا تطبّق migration RLS — فقط أنشئ الملف.
- لا push إلى GitHub.
- madina-quran تبقى سليمة.
- typecheck + lint + build يجب أن تنجح.
- إذا شعرت أن الجلسة تقترب من نهايتها، توقف بعد Layer 1 أو Layer 3
  واكتب تقريراً مؤقتاً.

## 🎯 نقطة توقف

بعد إتمام Layer 1 + Layer 3 + Hardening + Phase D:
- أوقف.
- اكتب "Phase C+D complete".
- انتظر تعليمي قبل Phase E.

ابدأ الآن.